<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>

  <%= f.input :description, placeholder: "quantity of" %>
  <%= f.input :ingredient_id, collection: Ingredient.all %>

  <%= f.button :submit %>
<% end %>
<br>
<%= link_to "Back", cocktail_path(@cocktail) %>
